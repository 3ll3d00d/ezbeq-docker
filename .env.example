# Copy this file to .env and fill in the values.
# .env is git-ignored so your local settings won't be committed.

# Path to your local ezbeq source tree / worktree (required)
EZBEQ_SRC=/path/to/ezbeq

# Path to your ezbeq.yml config file (default: ~/.ezbeq/ezbeq.yml)
# EZBEQ_CONFIG=/Users/you/.ezbeq/ezbeq.yml

# Port override — normally NOT needed.
# bin/run-local auto-reads the 'port:' value from your ezbeq.yml and uses it
# for the Docker port mapping.  Only set this if your port line is commented out
# in ezbeq.yml but you still want a non-default port.
# EZBEQ_PORT=9968
